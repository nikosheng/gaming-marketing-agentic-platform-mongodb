# ---- MongoDB ----------------------------------------------------------------
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?retryWrites=true&w=majority
MONGODB_DB=casino_marketing_demo
# Temporary workaround for TLS handshake/cert issues in non-production envs.
# WARNING: This disables certificate and hostname verification.
MONGODB_TLS_INSECURE=false
VECTOR_EMBEDDING_DIM=1024

# ---- Seeding ----------------------------------------------------------------
SEED_PATRON_COUNT=300
SEED_TABLE_COUNT=30

# ---- LiteLLM gateway (chat only) -------------------------------------------
# Embeddings do NOT go through LiteLLM anymore; they call the voyageai Python
# SDK in-process. LiteLLM is retained for chat routing / caching / observability.
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=sk-litellm-change-me
LLM_CHAT_MODEL=chat-primary

# ---- Voyage embeddings (in-process, local) ---------------------------------
# voyageai[local] requires no API key when the local weights are used.
# Set VOYAGE_API_KEY only if you want to call the hosted API as a fallback.
LLM_EMBEDDING_MODEL=voyage-4-nano
VOYAGE_API_KEY=

# ---- Optional: warm-up embedding on server start ---------------------------
EMBED_WARMUP_ON_START=true

# ---- Server binding --------------------------------------------------------
HOST=0.0.0.0
PORT=8000
